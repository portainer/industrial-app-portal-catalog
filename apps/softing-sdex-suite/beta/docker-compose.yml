services:
  sdex-suite:
    image: harbor.portainercloud.io/edge/softingindustrial/sdex-suite:((PIAP_IMAGE_VERSION))
    restart: always
    ports:
      - 443:443
      - 4897:4897
    volumes:
      - config:/config
      - mqtt-config:/mqtt

volumes:
  config: {}
  mqtt-config: {}
