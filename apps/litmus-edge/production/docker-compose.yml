services:
  litmus-edge:
    image: "litmusedge.azurecr.io/litmusedge-std-docker:((PIAP_IMAGE_VERSION))"
    ports:
      - "8443:443"
    cap_add:
      - NET_ADMIN
    volumes:
      - litmus-edge-data:/var

volumes:
  litmus-edge-data:
